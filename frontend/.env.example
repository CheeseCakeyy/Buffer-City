# Copy to .env.local for local development. Vercel: enter in project settings.
# These are SERVER-ONLY. Never use the NEXT_PUBLIC_ prefix for them.
BACKEND_URL=http://127.0.0.1:8000
# Match Render's BACKEND_PROXY_SECRET (32+ random characters in production).
BACKEND_PROXY_SECRET=
